import NavBar from "../pure/nav/NavBar";
/**
 * * This function allow to define the logic of the component.
 * * Each child will have all logic that is passed for this parent component. 
 * @returns React component

 */
export default function NavContainer(): JSX.Element {
  return <NavBar />;
}
